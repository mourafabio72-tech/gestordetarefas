# Conformidade com a vault

Uma linha por REGRA, não por fase. `Status` só vira `ok` com a saída real do
comando colada em `Evidência`, ou com a frase `conferência visual feita em <data>,
tela <qual>` quando a prova for visual. Fase não fecha com linha pendente.

| Nota | Regra literal | Proibido | Onde aplica | Prova | Evidência | Status |
|---|---|---|---|---|---|---|
| Padrao_Impersonacao_Segura | "Marcar como usado ANTES do login, e nunca depois" | emitir o JWT e só então marcar o jti | rota de consumo | teste: bilhete válido consumido, jti já consta usado antes de o token existir | Item 10 da `backend/provas/prova_sso_f3.py`: `ok 10. jti fica gravado mesmo quando nenhuma sessão é emitida`. A prova é um bilhete cujo e-mail não tem cadastro aqui: a rota recusa a entrada E o jti fica na tabela, o que só acontece se a marcação vem antes de existir sessão. Código em `routes/auth.py:135-137` (marca) contra `:161` (emite) | ok |
| Padrao_Impersonacao_Segura | "O `AND usado=0` faz o banco decidir o vencedor com a trava de linha" mais conferir linhas afetadas | `UPDATE` sem `AND usado=0`, ou sem checar rowcount | rota de consumo | duas requisições simultâneas com o mesmo bilhete: exatamente uma entra | Item 11: `ok 11. duas requisições simultâneas com o mesmo bilhete: só UMA entra` (duas threads reais com `threading.Barrier`, saída exigida `[200, 404]`). Forma diferente da nota, e declarada: o consumidor não tem a linha antes do bilhete chegar, então o equivalente ao `UPDATE ... AND usado=0` é `INSERT ... ON CONFLICT (jti) DO NOTHING` com `res.rowcount == 1` (`routes/auth.py:90-96`). Quem decide é o banco, e o código obedece. Trocando `:96` por `return True`, a prova falha nos itens 4, 5, 6 e 11 | ok |
| Padrao_Impersonacao_Segura | "Inexistente, expirado e já usado devolvem a MESMA resposta" | mensagem distinta por caso | rota de consumo | 4 chamadas (inválido, expirado, usado, assinatura errada) devolvem corpo e status idênticos | Itens 5, 6 e 7, rodados sobre NOVE casos e não quatro (lixo, expirado, já usado, chave errada, salt alheio, sem cadastro, bloqueada, inativa, convite pendente): `ok 5. todas as recusas devolvem 404, nunca 403`, `ok 6. todas as recusas devolvem o MESMO corpo, sem dizer o motivo` (o teste exige um único texto distinto entre as nove), `ok 7. a mensagem não revela se o problema é o bilhete ou a conta`. Uma constante só, `routes/auth.py:22` | ok |
| Padrao_Impersonacao_Segura | "Recusa token acima do tamanho esperado ANTES de consultar o banco" | validar tamanho depois da consulta | rota de consumo | bilhete de 10 KB recusado sem query no banco | Item 9: `ok 9. bilhete acima do teto é recusado sem nenhuma query no banco`. Não confia na leitura do código: um ouvinte de `before_cursor_execute` conta as queries e o teste exige ZERO. Código em `routes/auth.py:114-117`, primeira coisa depois de ler o IP | ok |
| Padrao_Impersonacao_Segura | "O token NUNCA entra em log" | bilhete em log, print ou mensagem de erro | Hub e Tareffas | `grep -rn "sso" --include="*.py" . \| grep -i "log\|print"` sem o valor do bilhete | LADO HUB: o grep devolve 8 linhas, todas falso positivo (a palavra "logado", cabeçalhos de prova, e o comentário de `app.py:473` dizendo que o bilhete não entra). Nenhuma passa o bilhete a log ou print. Prova executável, mais forte que o grep: item 7b da `provas/prova_sso_f2.py` confere que o valor do bilhete emitido não aparece em nenhuma linha da tabela `acessos`. A nota exige também "grava QUEM gerou e o IP": era o que faltava, e o IP passou a ser gravado nos 4 caminhos de entrada (item 12 da prova). LADO TAREFFAS: `grep -n "log_event\|print" backend/app/routes/auth.py | grep -i "bilhete\|body."` volta vazio, e o item 16 da prova é mais forte que o grep: varre `sso_bilhetes_usados` e `login_tentativas` e exige que nenhum pedaço do bilhete apareça (`ok 16`). O IP de quem consumiu é gravado nas duas tabelas (itens 12 e 17) | ok |
| Padrao_Impersonacao_Segura | "Redireciona logo após consumir" | bilhete permanecer na barra de endereço | frontend | após entrar, `window.location.search` está vazio | `frontend/src/contexts/bilhete.js:26-34` tira o bilhete da query e chama `history.replaceState` com o endereço limpo. Item 3 da `frontend/provas/prova_sso_f4.js`: `ok 3. a URL é limpa e o bilhete some do endereço`, com o teste conferindo o endereço que o `replaceState` recebeu, e não o código. A limpeza roda ANTES da chamada ao servidor, o que é mais do que a nota pede: recusa também limpa. Itens 4 e 7 provam que outros parâmetros, o hash e a rota sobrevivem. Comentando `bilhete.js:34`, o item 3 falha. CONFERÊNCIA VISUAL em produção, feita em 2026-08-16, tela: entrada pelo card do Hub. A barra de endereço logo após entrar mostrava `https://gestordetarefas.zoaria.com.br/`, sem `?sso=` e sem resto de query | ok |
| Padrao_IDOR | "Retornar 403 em vez de 404 quando não é dono: confirma que o recurso existe" | 403 na recusa do SSO | rota de consumo | resposta de recusa é 404 | Item 5: `ok 5. todas as recusas devolvem 404, nunca 403`. Os dois únicos pontos de recusa são `routes/auth.py:77` e `:117`, ambos 404. O 429 do limite de força bruta é a única outra saída, e não é recusa de bilhete | ok |
| Forca_Bruta_Login | "Mensagem de erro é única: nunca dizer se foi email ou senha que errou" | dizer "usuário não cadastrado" ou "usuário bloqueado" | rota de consumo | os três casos devolvem texto idêntico | Itens 7 e 8: `ok 8. conta bloqueada, inativa e com convite pendente são barradas`, todas pelo mesmo `RECUSA_SSO`. O item 7 checa que a mensagem não contém "cadastr", "bloquead", "inativ", "expirad" nem "usado". Entrou um quarto caso além dos três previstos: convite de primeiro acesso pendente (`routes/auth.py:156-157`) | ok |
| Forca_Bruta_Login | "Toda tentativa, sucesso ou falha, é registrada" | consumo sem rastro | rota de consumo | linha na tabela de tentativas após sucesso e após falha | Itens 12 e 13: `ok 12. tentativa bem sucedida é registrada com o IP de quem entrou`, `ok 13. tentativa recusada também é registrada`. Tabela `login_tentativas` (`models.py:314-321`), gravação em `seguranca.py:68-78`. O limite em si: item 14 (`depois de 5 falhas o mesmo IP recebe 429`) e item 15 (`o bloqueio é do IP que errou, e não do sistema inteiro`). FECHADA pela Fase 7 em 2026-08-17: o login por e-mail e senha passou a registrar e limitar igual ao SSO (`routes/auth.py:40-65`), com os mesmos 5 em 15 minutos. Itens 1 a 8 da `provas/prova_seguranca_f7.py`, entre eles o 4 (`depois de 5 falhas o mesmo IP recebe 429`) e o 5 (`bloqueado, nem a senha certa entra`), que prova que o corte vem ANTES do `verify_password`. PROVADO NO AR em 2026-08-17, com e-mail inexistente para não travar conta de ninguém: seis tentativas seguidas contra `gestordetarefas.zoaria.com.br` devolveram `401, 401, 401, 401, 401` e então `429 {"detail":"Muitas tentativas. Aguarde 15 minutos."}` na sexta. CONFERÊNCIA VISUAL na mesma data, tela de login: a sétima, feita pelo formulário, trouxe a faixa vermelha com esse texto acima do campo de e-mail | ok |
| Forca_Bruta_Login | regra 1: "Conta falhas por email OU IP (não só uma das duas)" | contar só por IP, que a máquina do atacante troca de graça | login por senha e SSO | e-mail que estourou o limite não entra nem de um IP novo | Item 6b: `ok 6b. o e-mail que estourou o limite não entra nem de um IP novo`, e item 6 mostrando que outra pessoa de outro IP entra normal, ou seja, o bloqueio discrimina. PREÇO DECLARADO, que é da regra e não da implementação: dá para travar a conta de outra pessoa errando a senha dela cinco vezes de qualquer IP. A nota manda contar pelos dois e a vault vence, mas o efeito fica escrito | ok |
| Forca_Bruta_Login | regra 4: "Tempo de bloqueio é configurável, sem redeploy" | número de segurança que qualquer um afrouxa pela tela | parâmetros | o valor sai de configuração e não de constante | DESVIO DECLARADO, herdado da Fase 3 e repetido na 7: `MAX_TENTATIVAS` e `JANELA_MINUTOS` são constantes em `seguranca.py:24-25`, e mudá-las exige deploy. Motivo escrito no comentário de `:21-23`: valor de segurança que só muda com deploy é problema menor do que valor de segurança editável pela tela por qualquer perfil. O projeto não tem tela de parâmetros de segurança, e criar uma seria ampliar escopo | desvio declarado |
| CSRF_Cookies_Headers | "Headers de segurança (em toda resposta)", a lista de seis | responder sem nenhum deles | nginx do frontend e API | `curl -I` mostra os seis, e a resposta de erro também | Antes desta fase NÃO EXISTIA NENHUM dos dois lados. LADO API: `seguranca.py:109-131` aplicados por middleware em `main.py:51-55`. Itens 9 e 10 da `prova_seguranca_f7.py`: `ok 9. resposta comum leva os 6 cabeçalhos de segurança` e `ok 10. resposta de erro também leva os cabeçalhos`. Item 17 prova que o middleware não trunca download binário (assinatura `PK` e `content-length` conferidos). LADO NAVEGADOR: `frontend/nginx.conf:14-22`, os seis no `location /` com `always`, conferidos pelos itens 1 a 4 da `frontend/provas/prova_headers_f7.py`. CSP diferente em cada lado, de propósito: a da API é `default-src 'none'` porque resposta de API não carrega recurso; a do site é `default-src 'self'` com `script-src 'self'`. Exceção declarada: `/docs`, `/redoc` e `/openapi.json` ficam fora da CSP da API (`seguranca.py:121`), porque a documentação carrega script e estilo de CDN. CONFERÊNCIA VISUAL em 2026-08-17, tela de login servida com a CSP real lida do `nginx.conf`: a tela abriu completa e o console do navegador ficou sem uma única violação. PROVADO NO AR em 2026-08-17 por `curl -sI` no domínio real, que é o que faltava: o site (HTTP 200) devolve os seis com a CSP `default-src 'self'`, e a API devolve os seis com `default-src 'none'` numa resposta de ERRO (HTTP 405), o que fecha de uma vez o "em toda resposta". O console do navegador na tela de login de produção não acusou nenhuma violação de CSP. Com isso cai a ressalva anterior sobre a sintaxe do `nginx.conf`: ela está provada por consequência, porque o container subiu e está servindo os cabeçalhos | ok |
| CSRF_Cookies_Headers | CORS não faz parte da nota, e a lista aberta é o que ela combate | `allow_origins=["*"]` com `allow_credentials=True` | API | origem não listada não recebe permissão | `main.py:29-45`: lista explícita vinda de `CORS_ORIGINS`, e `allow_credentials=False` porque não há cookie neste projeto. Itens 13, 14, 15 e 16 da prova. `"*"` com `allow_credentials=True` é configuração inválida que o Starlette contorna ecoando a origem, o que é pior do que parecia escrito. Item 13b cobre a armadilha do Compose, que entrega a variável como string vazia | ok |
| Vazamento_de_Chaves | "Toda chave, senha, token ou URL sensível vai em variável de ambiente" | chave do SSO literal no código | Hub e Tareffas | `grep -rn "ZOARIA_SSO_SECRET" --include="*.py" .` só acha leitura de ambiente | LADO HUB: 4 linhas, nenhuma com valor. `config.py:34` é `os.environ.get("ZOARIA_SSO_SECRET", "").strip()`; `sso.py:94` é comentário; `provas/prova_sso_f2.py:7` é docstring e `:28` define chave de teste no ambiente da própria prova. O Hub não tinha `.env.example` e passou a ter, com as 15 variáveis do código e nenhum valor real. LADO TAREFFAS: `grep -rn "ZOARIA_SSO_SECRET" --include="*.py" backend/` devolve 2 linhas, nenhuma com valor: `app/sso.py:25` (`os.getenv("ZOARIA_SSO_SECRET", "").strip()`) e `provas/prova_sso_f3.py:30` (define a chave de teste no ambiente da própria prova). A variável entrou no `.env.example:11-14` sem valor e no `docker-compose.yml:23-24` como `${ZOARIA_SSO_SECRET:-}` | ok |
| CSRF_Cookies_Headers | CSRF em todo POST, com rotas isentas declaradas explicitamente | rota de consumo sem estar na lista de isentas | Tareffas | a rota consta na lista de isentas, e as demais seguem exigindo token | NÃO SE APLICA a este projeto, e declarado em vez de fingido: `grep -rni "csrf" backend/app` volta vazio, porque o Tareffas não usa cookie de sessão. O token vai em `Authorization: Bearer` a partir do `localStorage` (`frontend/src/contexts/AuthContext.jsx:11,32`), e cabeçalho não é anexado sozinho pelo navegador em pedido de outro site. Não existe lista de isentas onde inscrever a rota. Motivo escrito na docstring da própria rota (`routes/auth.py:106-110`) | não se aplica |
| Timeout_de_Sessao | vida do cookie e inatividade são independentes | JWT mais longo por ter vindo do Hub | rota de consumo | `exp` do JWT do SSO igual ao do login por senha | Item 2: `ok 2. JWT do SSO expira junto com o do login por senha (diferença < 5s)`. O teste decodifica os dois JWTs e compara o `exp`, em vez de confiar na leitura do código. `routes/auth.py:161` chama `create_access_token` sem `expires_delta`, igual à rota de senha em `:39` | ok |
| App_Online_Auth | "Tela de login" é item obrigatório do tipo | remover ou esconder o login por senha | Tareffas | entrar pela URL direta ainda pede e-mail e senha | Itens 3, 20 e 21: `ok 3. login por senha segue funcionando ao lado do SSO`, `ok 20. com ZOARIA_SSO_SECRET vazia nada entra pelo SSO`, `ok 21. SSO desligado não derruba o login por e-mail e senha`. A rota `/api/auth/login` não foi tocada nesta fase | ok |
| CLAUDE.md da vault (regra 1) | "Nunca use travessao (em-dash)" e acentuação completa no que o usuário lê | travessão em mensagem de tela, inclusive em JSX | frontend | `grep -rn "—" frontend/src` volta vazio | FECHADA pela Fase 6 em 2026-08-17. `grep -rn "—" frontend/src` devolve **saída vazia, exit code 1**, contra as 46 ocorrências em 13 arquivos que esta linha registrava. Foram 42 trechos reescritos (os 46 caracteres cabem em 42 linhas porque quatro `<option>` tinham travessão duplo emoldurando a opção nula), e o `git diff --stat` do frontend fecha em 42 inserções contra 42 remoções, simetria que prova reescrita no lugar em vez de linha criada ou apagada. Três formas, escolhidas por leitura de cada frase: dois pontos ou vírgula quando separava oração, parêntese quando emoldurava opção nula (`(não vinculada)`, `(sem responsável)`, `(escolher)`), e hífen simples quando era valor ausente de célula (nunca en-dash, que é aviso A2 do Pasquale). Verificado junto: `grep -rn "–" frontend/src` vazio (en-dash), mojibake vazio por `grep -rlnP`, e `file -I` devolvendo `charset=utf-8` nos 13 arquivos. CAUSA RAIZ ATENDIDA, e é o achado que mais importa aqui: a frase de `Tarefas.jsx:224` também é emitida pelo servidor em `backend/app/routes/tarefas.py:257`, e limpar só o React deixaria o travessão saindo pelo outro caminho. A varredura do backend por texto lido pelo usuário achou 7 casos, todos fechados, e `grep -rn "—" backend/app` cruzado com `detail=\|"message"\|prazo_label` volta vazio. Regressão: 7 casos da `prova_sso_f4.js`, 25 da `prova_sso_f3.py`, 7 do `app/sso.py` e `npm run build` com os mesmos 2276 módulos. NÃO COBERTO, e declarado: sobram 44 travessões em 17 arquivos de `backend/app`, todos comentário e docstring, que o usuário não lê e que estão na mesa do usuário; e o executor `scripts/pasquale.py` não existe nesta cópia da vault, então a parte de dicionário de acentuação que só ele cobre não foi varrida | ok |
| Padrao_CLAUDE_MD_Projeto | "Todo projeto de código DEVE TER um CLAUDE.md na raiz" com o bloco que aponta para a vault | projeto seguir sem o arquivo | raiz do Tareffas | o arquivo existe e abre com o bloco padronizado | `ls -la CLAUDE.md` devolveu `-rw-r--r-- 9787 Aug 16 18:47 CLAUDE.md`; `head -6` mostra o título na linha 1 e o bloco `> Antes de TUDO: existe uma vault Obsidian em /Users/fabiomoura/ObsidianJovi/` nas linhas 3-6. Os 5 caminhos citados no bloco foram testados com `[ -f ]`: 5 de 5 existem | ok |

## Não coberto, e declarado

- `70_ESTILO/` não existe nesta cópia da vault. O eixo de voz e vocabulário
  proibido não foi varrido, então nenhuma linha acima alega conformidade com ele.
- `scripts/pasquale.py` não existe nesta cópia da vault (`ls scripts/` devolve só
  `atualizar_mapa.py`). O gate de língua rodou pelos greps de complemento que a
  própria `Revisao_Professor_Pasquale` prevê para máquina sem Python à mão, mais
  leitura do diff. O que fica sem varredura é o dicionário de palavra sem acento
  em texto visível, códigos E5 e E6, que só o script cobre.
- 32 travessões em 13 arquivos de `backend/app`, todos em comentário e docstring
  (número corrigido na segunda rodada da Fase 6: os 44 anteriores vinham de uma
  varredura fraca). `Sem_Travessao` os alcança, e nenhuma tela muda por causa
  deles: AST devolve zero string de dado e o OpenAPI devolve zero. Decisão do
  usuário, registrada no LOG da Fase 6.
- A sintaxe do `frontend/nginx.conf` não foi validada por `nginx -t`: esta
  máquina não tem nginx nem docker. RESOLVIDO POR OUTRO CAMINHO em 2026-08-17: o
  `curl -sI` em produção mostra os seis cabeçalhos saindo do nginx real, e nginx
  com configuração inválida não sobe. O que continua sem prova direta é o
  `nginx -t` em si.
- As cinco tentativas do meio, no teste de limite em produção, saíram por `fetch`
  de mesma origem e não pelo formulário, porque os cliques pararam de responder
  no painel do navegador. O servidor está provado seis vezes; o formulário, duas
  (a primeira, com 401, e a última, com o aviso do 429 na tela).

## Fechamento

Em 2026-08-17, depois da Fase 7, a matriz tem 19 linhas e nenhuma pendente:
17 `ok`, 1 `não se aplica` (CSRF, por não haver cookie de sessão neste projeto) e
1 `desvio declarado` (limite de tentativa em constante, e não em configuração
editável). A linha que estava `parcial (SSO ok)` fechou: o login por e-mail e
senha passou a registrar e limitar igual ao SSO.

A última pergunta que o repositório não respondia foi respondida pelo painel em
2026-08-17: o backend NÃO tem domínio próprio. Os dois domínios do projeto
apontam para a porta 80 (o nginx do frontend), e o backend responde em 8000 sem
ser destino de nenhum. Logo `/docs` e `/openapi.json` não saem pela internet, e
`docs_url=None` não precisa entrar.

---

# Trabalho: varios responsaveis por (empresa, setor) — aberto em 2026-09-09

Linhas novas. As de cima sao do SSO, fechadas em 2026-08-17, e ficam como estao.

| Nota | Regra literal | Proibido | Onde aplica | Prova | Evidencia | Status |
|---|---|---|---|---|---|---|
| Padrao_IDOR | "Todo endpoint que receber ID de recurso na URL ou no body deve validar que o usuario autenticado tem direito ao recurso ANTES de retornar dado. Sem excecao." | `db.add()` com setor_id ou responsavel_id vindo do body sem checagem previa | PUT `/empresas/{id}/responsaveis-setor` | prova executavel: id inexistente e id de usuario `tipo=cliente` sao recusados | Itens 6, 7, 8 e 9 da `backend/provas/prova_responsaveis_multiplos.py`: `ok 6. id de pessoa inexistente e recusado`, `ok 7. usuario do tipo cliente e recusado`, `ok 8. usuario bloqueado e recusado`, `ok 9. setor inexistente e setor inativo sao recusados`. Codigo em `routes/empresas.py:164-183`, TODO antes do primeiro `delete` da linha 185. O item 11 e o que prova a ORDEM, e nao so a existencia da checagem: `ok 11. recusa nao altera o que ja estava gravado`. Verificado que a prova REPROVA sem a checagem: removendo o bloco 164-183, falham os itens 6, 7, 8, 9 e 11 (saida colada no LOG de 2026-09-09). De quebra, o GET da mesma matriz saiu de `get_current_user` para `require_perm("empresas", "ver")` (`:122`): achado do verificador de conformidade, porque usuario `tipo=cliente` de outra empresa lia a matriz inteira | ok |
| Padrao_IDOR | "retorna 404 (nao 403, pra nao revelar que o recurso existe)" | 403 na recusa | mesma rota | a prova confere o status da recusa | Item 10: `ok 10. recusa e 404, e a mensagem nao diz se o id existe ou so nao serve`. O teste exige duas coisas, e a segunda entrou depois do verificador apontar que a primeira sozinha era tautologia (a rota nao usa 403 em lugar nenhum): o conjunto de status das cinco recusas tem que ser exatamente `{404}`, E o `detail` tem que ser UM so entre id inexistente, usuario cliente e usuario bloqueado. Sem isso o texto viraria oraculo: quem varre ids descobre quem e do sistema pela mensagem | ok |
| Padrao_Mass_Assignment | "Backend define a whitelist de campos editaveis. Tudo que veio no body e nao esta na whitelist eh ignorado." | schema sem `extra = "forbid"` | schema do body da matriz | `grep -n "forbid" backend/app/routes/empresas.py` | Duas linhas, uma por schema: `40:    model_config = ConfigDict(extra="forbid")` (RespSetorItem) e `88:` (RespSetorBody). Prova executavel alem do grep, item 14: `ok 14. campo inventado no corpo e recusado`, mandando `responsavel_id_extra` e `atende` no item e exigindo 422 | ok |
| Padrao_Validacao_de_Input | "Toda string crua passa por validacao tipada antes de tocar regra de negocio." | lista de ids indo direto para o ORM | mesma rota | a validacao esta no schema Pydantic, antes da funcao da rota | As tres camadas da nota, cada uma no seu lugar: tipo e teto no Pydantic, antes da funcao (`routes/empresas.py:44-45`, `List[int]` com `max_length=MAX_RESP_POR_SETOR`); forma no validador do campo (`:60-73`, id repetido colapsa); regra na rota (`:164-183`, existe e e elegivel). Itens 13 e 12: `ok 13. lista acima de 20 e recusada` (422, vindo do schema) e `ok 12. id repetido colapsa, mantendo a primeira posicao`. O teto e decisao local declarada, com o motivo escrito em `:31-36`: a vault nao tem regra de tamanho de lista em JSON | ok |
| Padrao_Logging_Estruturado | "Tentativa de auth (sucesso e falha), 403 e mutacao de dado critico SEMPRE entram." | regravar a matriz sem log | mesma rota | `grep -n "log_event" backend/app/routes/empresas.py` | Duas chamadas, e a segunda e achado do verificador: `205` (PUT da tela) e `115` (importacao por planilha, que regrava a MESMA matriz em muitas empresas de uma vez e nao logava nada). Linha real, capturada rodando a prova: `{"timestamp": "2026-09-09T10:18:52.705596-03:00", "level": "INFO", "event": "EDICAO_REGISTRO_CRITICO", "tabela": "empresa_setor_responsavel", "empresa_id": 1, "user_id": 1, "ip": "testclient", "setores": 1, "responsaveis": 2}`. Sem senha, sem token, sem nome e sem e-mail: so ids e contagem. NAO COBERTO, e declarado: `request_id`, `path` e `method`, que a nota lista como obrigatorios em toda linha, nao existem no `log_event` deste projeto (`seguranca.py:57`) desde a Fase 7. Acrescenta-los e mexer no logger central de um app inteiro, o que nao tem relacao com responsavel multiplo; fica registrado aqui e no LOG em vez de corrigido em silencio | desvio declarado |
| Padrao_Selecao_em_Lote | "`.tsel{ appearance:none; ...}` Nunca o do sistema operacional." | checkbox cru do sistema operacional no popover | `components/SeletorResponsaveis.jsx` | `grep -rn 'type="checkbox"' frontend/src/components/SeletorResponsaveis.jsx` e conferir aparencia propria em toda ocorrencia | O grep devolve UMA ocorrencia, `SeletorResponsaveis.jsx:50`, e a linha seguinte e `className="check-app"`. A classe esta em `index.css:36-59`, no CSS central e nao na tela: `appearance-none` com o check desenhado num `::after` de borda em L girada 45 graus, exatamente a tecnica da nota, escrita em Tailwind pelo precedente de 2026-08-20. O `Empresas.jsx:541` (o "atende") tambem passou para `check-app`, que antes era `h-4 w-4` no checkbox cru. CONFERENCIA VISUAL feita em 2026-09-09, tela de cadastro de empresa rodando local: o quadrado marcado sai verde-oliva com o check branco, na cor da marca, e nao no azul do sistema | ok |
| Padrao_Toggle_OnOff | "Toggle e para ESTADO; checkbox e para MULTI SELECAO" | transformar o "atende" em switch | aba de responsaveis | o "atende" so grava no submit, logo continua checkbox | Aplicado o teste de uma pergunta da propria nota ("clicar nisso muda alguma coisa agora?"): o "atende" so altera estado local (`Empresas.jsx:541-543`) e a gravacao acontece em `handleSubmit`, logo e checkbox e continuou checkbox. O motivo esta escrito no codigo (`Empresas.jsx:533-537`), para a proxima varredura nao "consertar" isto para switch. Confirmado por verificador de contexto limpo | ok |
| Padrao_Toggle_Tipos | "Fora desta taxonomia. Os tres tipos acima sao para ESCOLHER ou LIGAR" | tratar o multi select como um dos 3 estilos de toggle, ou inventar um quarto | aba de responsaveis | CONFERENCIA_VISUAL | Conferido em 2026-09-09, tela de cadastro de empresa: o seletor e uma lista de checkbox dentro de um popover, com os escolhidos em chip na linha. Nao e switch, nao e segmented, e nao inventou um quarto estilo. A escolha do formato nao foi minha: veio da pergunta socratica 2 da Fase 8, respondida pelo usuario | ok |
| Padrao_Tabela | "as linhas sao registros comparaveis entre si? Se nao sao, e grade, nao tabela de listagem." | instrumentar a grade de setores com filtro por coluna, ordenar, exportar ou menu sanduiche | aba de responsaveis | `grep -n "data-col-key\|TabelaAvancada" frontend/src/pages/Empresas.jsx` | Saida vazia, exit code 1. E o comentario que a nota pede esta em `Empresas.jsx:533-537`, dizendo que sao setores de UMA empresa e nao registros comparaveis entre si, para a proxima varredura nao instrumentar e quebrar o significado. Nem sequer e `<table>`: e uma lista de `<div>` | ok |
| Padrao_Modal_Popup_Centrado | "SEM `overflow:visible` o dropdown do SelectBusca fica CORTADO pelas bordas do modal" | popover cortado pela borda do modal | modal de cadastro de empresa | CONFERENCIA_VISUAL com o popover aberto no ULTIMO setor da lista | CONFERIDO em 2026-09-09, tela de cadastro de empresa rodando local, popover aberto no ULTIMO setor (Societario), que e o caso que corta: o painel aparece INTEIRO, atravessando a borda de baixo do modal, sem recorte nenhum. A tecnica e a traducao da nota para este stack: o modal aqui e `<div>` com `overflow-y-auto`, e um filho posicionado seria cortado igual, entao o painel e desenhado com `createPortal` no `document.body` (`SeletorResponsaveis.jsx:184`), saindo do fluxo do modal em vez de pedir `overflow:visible` a ele. Motivo escrito no cabecalho do componente | ok |
| Padrao_Modal_Nao_Fecha_Sozinho | "O modal sai pelo X ou pelo Cancelar, e por mais nada." | ESC ou clique fora fechando o modal junto com o popover | modal de cadastro de empresa | CONFERENCIA_VISUAL, mais caso na prova Node do estado do popover | CONFERIDO em 2026-09-09, na tela: com o popover aberto e duas pessoas escolhidas, ESC fechou SO o popover e o modal continuou aberto com os chips no lugar; clique fora, dentro do modal, fez o mesmo. Provado tambem em Node, itens 13, 14 e 15 da `prova_seletor_responsaveis.js`, entre eles o que mais importa: com o popover JA fechado o ESC nao e do seletor, senao ele engoliria uma tecla que nao e dele. O modal em si nao tem handler de ESC nem de clique no backdrop (`Empresas.jsx:396`), entao ja obedecia | ok |
| Sistema_de_Estilos | "PROIBIDO: o hex aparecer em qualquer template" | `#5f7057` escrito no JSX | componente novo | `grep -rniE "#[0-9a-f]{6}" frontend/src/components/SeletorResponsaveis.jsx` | Saida vazia, exit code 1, nos dois arquivos novos (o `.jsx` e o `.js` da logica). Cor sai de classe Tailwind (`text-primary-600`, `bg-primary-50`), e o checkbox, que precisou de CSS proprio, foi para `index.css` usando `theme('colors.primary.600')` em vez de hex | ok |
| Icones_Phosphor | "Nada de emoji em UI. Sempre Phosphor." | emoji como icone | componente novo | `grep -rn "lucide-react" frontend/src/components/SeletorResponsaveis.jsx` | `3:import { Users, X } from 'lucide-react';`. A metade cumprida e a que importa aqui: zero emoji no componente. A biblioteca continua lucide, DESVIO DECLARADO no LASTRO, porque o app nasceu com ela nas 17 telas e trocar tudo nao tem relacao com responsavel multiplo. Usar Phosphor so no componente novo criaria a segunda linguagem de icone que a nota justamente combate | desvio declarado |
| CLAUDE.md da vault (regra 1) | "Nunca use travessao (em-dash)" no que o usuario le | travessao em JSX ou em `detail=` do backend | frontend e backend | `grep -rn "—" frontend/src` sem nenhuma ocorrencia em texto de tela, e AST do backend sem string de dado com travessao | FECHADA EM 2026-09-09, na mesma sessao, a pedido do usuario. A linha ficou algumas horas em `parcial` com o numero medido, e o numero era o que fazia ela fechavel: 51 ocorrencias em texto visivel, em 9 telas, mais 3 no backend. Agora ZERO nos dois lados. A medida nao e o grep cru: o grep conta comentario junto, e comentario ficou de proposito (decisao da Fase 6). O que se mede e o texto que sobra DEPOIS de tirar comentario de linha e de bloco no frontend, e no backend as strings de DADO pela AST, ignorando docstring. Os dois devolvem 0. Tres formas de reescrita, escolhidas por leitura de cada frase, as mesmas da Fase 6: dois pontos ou virgula quando separava oracao, parenteses quando emoldurava aposto (`no ZapContabil (achado pelo e-mail) e o`), e hifen simples em celula sem valor (`{d.protocolo || '-'}`). A ARMADILHA que este trabalho tinha, e que a Fase 6 ja tinha ensinado: a frase "Reconhecido: confira e salve" e emitida pelo servidor (`routes/modelos.py:145`) e COMPARADA com `===` pela tela (`Modelos.jsx:286`). Mudar um lado so faria o card verde sumir sem erro nenhum. Mudou nos dois, no mesmo commit, e ficou um comentario no servidor avisando. Verificador de contexto limpo leu o diff frase a frase e achou QUATRO frases que ficaram piores em portugues, todas corrigidas: uma sem separador nenhum entre duas oracoes, uma com dois "dois pontos" na mesma frase, uma com "que... que" redundante, e um "linha do escritorio: (nao configurada)". En-dash: zero. Charset: utf-8 nos arquivos tocados. `npm run build` ok, 24 provas do backend e 17 do frontend rc=0. PROVADO NO AR, e nao so pelo carimbo, que diz que a imagem e nova e nao que o texto entrou nela: o bundle servido por producao (`/assets/index-DRT8Tced.js`, 532K) e BYTE A BYTE identico ao build local, tem 1 ocorrencia de `Reconhecido: confira e salve` e ZERO de `Reconhecido — confira e salve` | ok |
| Verificacoes_Mecanicas_de_Tela | "Regra listada nao e regra cumprida. So vira cumprida quando alguem prova." | marcar item do checklist sem saida de comando | todas as fases | a coluna Evidencia desta tabela preenchida com saida real | Todas as linhas fechadas ate aqui tem saida de comando, arquivo e linha, ou a data e a tela da conferencia visual. Duas provas foram reescritas depois de um verificador mostrar que passavam por acidente: o item 10 da prova do backend (a rota nao usa 403 em lugar nenhum, entao "nenhuma recusa devolve 403" passaria com a validacao inteira removida) e o alcance desta linha do travessao, que estava escrita para o frontend todo e so e verdade nos arquivos desta fase. Nenhum item do CHECKLIST foi marcado sem evidencia apontavel | ok |
| Padrao_Logging_Estruturado | "mutacao de dado critico SEMPRE entra" | criar ou remover excecao de obrigacao sem registro | acao "nao se aplica" e a lista de excecoes | `grep -n "log_event" backend/app/routes/obrigacoes.py backend/app/routes/tarefas.py` | Os dois lados da decisao estao registrados: `routes/tarefas.py:804` na criacao (`EXCLUSAO_REGISTRO_CRITICO`, com obrigacao, empresa, tarefa, autor e IP) e `routes/obrigacoes.py:63` na remocao (`EDICAO_REGISTRO_CRITICO`, com `acao="removida"`). Nenhum dos dois carrega senha, token ou nome de pessoa: so ids. Continua valendo o desvio declarado da linha do PUT da matriz, sobre `request_id`, `path` e `method`, que nao existem no `log_event` deste projeto | ok |
| Escada_Preguica_de_Codigo | "2. Ja existe no codebase? -> reusa, nao reescreve" | status novo no enum quando `CANCELADA` ja resolve; componente de multi selecao novo quando ja existe um | fases 11 e 15 | `grep -n "NAO_SE_APLICA" backend/app/models.py` volta vazio, e o seletor da tela de Tarefas passa a vir de `components/` | As duas partes cumpridas. (a) `grep -n "NAO_SE_APLICA" backend/app/models.py` volta vazio: o enum `StatusTarefa` continua com os cinco de sempre, e "nao se aplica" e um campo ao lado de CANCELADA. O reuso pagou de imediato: o painel ja separava CANCELADA de pendente e atrasada, entao o item "nao conta como pendente nem atrasada" veio de graca. (b) `grep -c "responsavel_ids.includes" frontend/src/pages/Tarefas.jsx` devolve 0, e a tela agora importa `components/SeletorResponsaveis`, extraido do que ja existia | ok |
| Escada_Preguica_de_Codigo | "correcao de bug e na causa raiz: procure TODOS os chamadores da funcao que voce vai tocar" | tirar o fallback so no gerador e deixar `substituicao.py` e `Tarefas.jsx` chamando o responsavel da obrigacao | fase 13 | `grep -rn "obrigacao.responsavel\|o\.responsavel" backend/app frontend/src` | A varredura devolve UMA linha, e ela e um comentario (`gerador.py:300`, dizendo que o campo deixou de valer). Os tres chamadores foram no mesmo trabalho: gerador, `substituicao.py` e a tela. O quarto, que so apareceu na varredura, foi o PAYLOAD da tela: parar de mostrar o campo sem parar de MANDAR ele gravaria `null` por cima do valor legado de toda obrigacao editada | ok |

## Fases 18 e 19: campos obrigatórios no logger central (aberto em 2026-09-09)

| Nota | Regra literal | Proibido | Onde aplica | Prova | Evidência | Status |
|---|---|---|---|---|---|---|
| Padrao_Logging_Estruturado | a tabela de campos obrigatórios: `timestamp`, `level`, `event`, `user_id`, `ip`, `request_id`, `path`, `method` | linha de log saindo com menos que os oito dentro de um request | `log_event` de `backend/app/seguranca.py` | item (a) da `prova_logging.py`: linha emitida dentro de request tem os 8 campos | Item 1 da `prova_logging.py`: `ok 1. linha emitida dentro de request tem os oito campos da nota`, e item 2, `ok 2. os oito campos saem na ordem da tabela da nota`, que compara `list(linha.keys())[:8]` com a lista literal da nota. Linha real capturada no stdout: `{"timestamp": ..., "level": "INFO", "event": "LOGIN_OK", "user_id": 1, "ip": "testclient", "request_id": "d15c9c7f04774712", "path": "/api/auth/login", "method": "POST", "email": ...}`. O preenchimento e por `setdefault` em `seguranca.py:112-116` | ok |
| Padrao_Logging_Estruturado | "`request_id`: UUID gerado por request, ajuda a correlacionar logs" | id fixo, id por processo, ou id que repete entre requests | middleware | itens (b) e (c): dois requests diferem, duas linhas do mesmo request coincidem | Itens 5, 8 e 9: `ok 5. duas linhas do MESMO request tem o mesmo request_id`, `ok 8. dois requests distintos tem request_id diferente`, `ok 9. o request_id tem os 16 hexadecimais que a nota usa`. Gerado em `seguranca.py:79` com `uuid.uuid4().hex[:16]`, o mesmo da nota, e aberto pelo middleware `_contexto_de_log` de `main.py:66` | ok |
| Padrao_Logging_Estruturado | "Senha, token e PII NUNCA entram em log", com a lista absoluta | `Authorization` ou `Cookie` brutos entrando junto com os campos novos | `log_event` | item (f): nenhuma chave da lista proibida aparece na linha | Item 16: `ok 16. nenhuma chave da lista proibida aparece em linha nenhuma (achadas: [])`. O teste varre TODAS as linhas emitidas nas cinco capturas da prova e casa cada chave contra `senha, password, token, authorization, cookie, csrf, cpf, cartao, secret`. O contexto so carrega path, method, ip e id do usuario: cabecalho nenhum entra | ok |
| Mapa_de_Conceitos_de_Seguranca (Família 6, linha 175) | "**Anti-padrão**: `except Exception: pass` silencia erro sem log" | engolir erro na leitura do contexto para o log nunca falhar | `seguranca.py` | `grep -n -A1 "except Exception:" backend/app/seguranca.py` sem `pass` na linha seguinte | `grep -n -A1 "except Exception:" backend/app/seguranca.py` volta vazio: o arquivo inteiro nao tem um so `except`. A leitura do contexto nao precisa de guarda porque as tres `ContextVar` nascem com `default=None` (`seguranca.py:65-69`), entao `.get()` fora de request devolve None em vez de levantar `LookupError`. Item 10 da prova confirma pelo comportamento: `ok 10. log_event fora de request nao levanta excecao` | ok |
| Escada_Preguica_de_Codigo | "a correção preguiçosa É a correção de causa raiz. Uma guarda na função compartilhada é um diff menor do que uma guarda em cada chamador" | passar `request` para os 14 chamadores do `log_event` | fase 18 | `git diff --stat` da fase não toca os arquivos de rota por causa do logger | `git diff --stat` da fase: 4 arquivos de codigo, 90 linhas. Um unico arquivo de rota aparece, `routes/auth.py`, e nao por causa do `log_event`: e a migracao de `usuario_id=` para o campo automatico, item 18.5. Os outros 13 chamadores (empresas, obrigacoes, tarefas) NAO foram tocados e passaram a emitir os oito campos assim mesmo. A guarda ficou na funcao compartilhada, `seguranca.py:104` | ok |
| Escada_Preguica_de_Codigo | degrau 3, biblioteca padrão antes de dependência nova | instalar pacote de correlation-id | fase 18 | `git diff backend/requirements.txt` vazio | `git diff backend/requirements.txt` vazio. O carregador do contexto e `contextvars`, da biblioteca padrao (`seguranca.py:13`), e o id sai de `uuid`, tambem da padrao. Nenhum pacote de correlation-id entrou | ok |
| TDD_RED_GREEN_REFACTOR | "Em toda mudanca de logica de negocio ... escreve-se o teste ANTES do codigo. RED -> GREEN -> REFACTOR." | prova escrita depois, que passa de primeira e nunca reprovou nada | fase 18 | a `prova_logging.py` sai com código 1 ANTES do código novo, com a saída colada no LOG, e com 0 depois | RED registrado no LOG em 2026-09-09T18:26: `PROVA FALHOU nos itens: [1, 2, 3, 4, 5, 7, 8, 9, 11, 12, 13, 14, 15]`, exit 1, com a prova escrita ANTES do codigo. GREEN em 18:27: `PROVA OK: 16 checagens verdes`, exit 0. Dois itens foram endurecidos ainda no RED por passarem em falso verde (o 7 comparava dois ausentes entre si, o 13 dava por null um campo inexistente) | ok |
| Sem_Travessao | "Nunca usar o caractere travessão (em-dash, `—`) em lugar nenhum: Código, Comentários, Templates, Documentação" | travessão em comentário ou docstring dos arquivos novos | diff da fase | `grep -rn "—"` nos arquivos tocados volta vazio | `grep -rn "—" backend/app/` volta vazio nos cinco arquivos tocados. Um travessao foi encontrado e reescrito: comentario preexistente em `routes/auth.py:188`, que virou dois-pontos. ACHADO REGISTRADO E FORA DESTA FASE: o resto do backend tem cerca de 90 travessoes em comentario e docstring, que a varredura de 2026-09-09 nao pegou porque mediu pela AST so as strings de dado | ok |
| Fechar_Tarefa_Rodar_Verifica | "'Pronto' nao e uma palavra que se diz sozinho" | fechar a fase sem regressão e sem prova em produção | fase 19 | `prova_sso_f3.py` e `prova_seguranca_f7.py` verdes, mais carimbo de produção conferido contra o HEAD | As 25 provas de `backend/provas/` rodaram, e nao so as duas do item: 25 com exit 0. `prova_sso_f3.py` 25 verdes, `prova_seguranca_f7.py` 19 verdes, `prova_logging.py` 17 verdes. Em producao, `curl -s https://gestordetarefas.zoaria.com.br/api/health` devolve `{"status":"healthy","build":"20260909-1835"}`, e `git log -1 --date=format:'%Y%m%d-%H%M'` devolve `e3c14e6 20260909-1835`: o carimbo bate com o HEAD, entao a imagem no ar e a desta fase | ok |
| Decisão do usuário, 2026-09-09 | o campo chama `user_id`, e não `usuario_id` | `usuario_id` sobrando em linha de log | rotas | `grep -rn "usuario_id=" backend/app/routes/ \| grep log_event` volta vazio | `grep -rn "usuario_id=" backend/app/routes/ | grep log_event` volta vazio. As tres rotas de entrada (LOGIN_OK, SSO_ATIVOU_CONTA, SSO_OK) chamam `registrar_usuario(user.id)` antes do log, porque quem esta entrando ainda nao passou pela dependencia de autenticacao. Itens 14 e 15 da prova: `ok 14. a linha do LOGIN_OK traz user_id, a grafia da nota` e `ok 15. nenhuma linha carrega o campo legado usuario_id` | ok |
| Ampliação declarada, 2026-09-09 | `X-Request-ID` volta na resposta, com o mesmo valor da linha de log | cabeçalho ausente, ou com valor diferente do log daquele request | middleware | `curl -sI` em produção duas vezes, valores diferentes entre si e iguais ao log do request | `curl -sI https://gestordetarefas.zoaria.com.br/api/health | grep -i x-request-id` em tres chamadas seguidas: `9543699a3cbc4512`, `a325449b76254a10`, `15c1ff5d47e347a4`. Tres valores diferentes, 16 hexadecimais cada. A igualdade com a linha de log daquele request e provada em maquina pelo item 7 da `prova_logging.py`, que compara o cabecalho com o `request_id` da linha capturada no stdout | ok |

Em 2026-09-09, com as fases 20 e 21 abertas, entram seis linhas novas, todas
pendentes:

| Nota | Regra literal | Proibido | Onde aplica | Prova | Evidência | Status |
|---|---|---|---|---|---|---|
| CSRF_Cookies_Headers | a seção chama-se "Headers de segurança (em toda resposta)", e a lista traz `X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy`, `Strict-Transport-Security`, `Permissions-Policy` e `Content-Security-Policy` | resposta de erro 500 saindo sem cabeçalho nenhum, que é o estado de hoje | handler de erro, fase 20 | item (d) da `prova_erro_500.py`: a resposta de uma rota que levanta exceção traz os cabeçalhos | Item 4 da `prova_erro_500.py`: `ok 4. a resposta de erro traz os cabecalhos de seguranca (faltando: [])`. A lista conferida e a propria `HEADERS_SEGURANCA` importada de `app/seguranca.py`, e nao uma copia dentro da prova: se um cabecalho sair da nota e do codigo, a prova acompanha. Na versao anterior o mesmo item devolvia os seis como faltando | ok |
| Revisao_Vulnerabilidades | item 9, e a Família 6 do mapa: "o cliente recebe pouca informação, o log do servidor recebe muita" | `str(exc)`, traceback, caminho de arquivo ou SQL no corpo devolvido ao cliente | handler de erro, fase 20 | item (e): o corpo não contém `Traceback`, nem o nome da exceção, nem caminho de arquivo | Item 5: `ok 5. o corpo nao carrega traceback, excecao, caminho nem SQL (achados: [])`. A excecao levantada na prova carrega de proposito `SELECT senha_hash FROM usuarios WHERE email='chefe@bps4.com.br'`, e o corpo devolvido e `{"detail": "Erro interno. Tente novamente."}`. O nome da excecao vai para a LINHA DE LOG (`excecao: ValueError`) e nao para o cliente, que e a Familia 6 na pratica | ok |
| Padrao_Logging_Estruturado | a tabela dos oito campos, agora na linha de erro | `ERRO_NAO_TRATADO` saindo sem `request_id`, que é justamente o caso em que ele mais serve | handler de erro, fase 20 | itens (c) e (f): o `X-Request-ID` da resposta é igual ao `request_id` da linha, e a linha tem os oito campos | Itens 3, 8 e 9: `ok 3. o X-Request-ID do erro e o mesmo id da linha de log daquele request`, `ok 8. a linha ERRO_NAO_TRATADO tem os oito campos da nota`, `ok 9. a linha do erro liga com a resposta: mesmo request_id, path e method`. Linha real: `{'level': 'ERROR', 'event': 'ERRO_NAO_TRATADO', 'user_id': null, 'ip': 'testclient', 'request_id': 'edd6329ded6f47bd', 'path': '/api/_prova_estoura', 'method': 'GET', 'excecao': 'ValueError'}`. RESSALVA REGISTRADA: os oito campos saem, mas o `user_id` sai `null`, porque o handler roda em contexto ancestral ao da task do request. Nao e conserto de uma linha: exigiria `get_current_user` receber o `request` | ok |
| Escada_Preguica_de_Codigo | "2. Ja existe no codebase? -> reusa, nao reescreve" | escrever lista de cabeçalhos nova no handler quando `aplicar_headers` já existe | fase 20 | `grep -n "aplicar_headers" backend/app/main.py` mostra reuso | `grep -n "aplicar_headers" backend/app/main.py` devolve tres linhas: a importacao em `:7`, o middleware em `:62` e o handler novo em `:111`. Nenhuma lista de cabecalho nova foi escrita | ok |
| TDD_RED_GREEN_REFACTOR | "escreve-se o teste ANTES do codigo. RED -> GREEN -> REFACTOR" | prova escrita depois, que passa de primeira | fase 20 | `prova_erro_500.py` com exit 1 antes e 0 depois, as duas saídas coladas no LOG | RED colado no LOG das 20:40:00: `PROVA FALHOU nos itens: [2, 3, 4, 6, 7, 8, 9]`, exit 1 contra o codigo anterior. GREEN no LOG das 20:40:45: `PROVA OK: 14 checagens verdes`, exit 0. Os itens que ja passavam no RED estao nomeados no LOG um a um, com o motivo, e tres deles sao itens de nao-regressao medidos ANTES de proposito | ok |
| Sem_Travessao | "Nunca usar o caractere travessão (em-dash, `—`) em lugar nenhum: Código, Comentários, Templates, Documentação" | medir só as strings de dado, que foi o erro da varredura de 2026-09-09 de manhã | fase 21 | `grep -rn "—" backend/app backend/provas frontend/src` volta vazio | | pendente |

Em 2026-09-09, com a fase 22 aberta, entram tres linhas novas, todas pendentes:

| Nota | Regra literal | Proibido | Onde aplica | Prova | Evidência | Status |
|---|---|---|---|---|---|---|
| Padrao_Logging_Estruturado | a tabela de anti-padrões traz "`except Exception: pass`: Erro acontece e ninguém sabe", e a nota abre dizendo que "Log é a única coisa que sobra quando dá problema" | 45 erros falsos por boot, que enterram o erro verdadeiro; ou o contrário, silenciar tudo para o log ficar limpo | `migrate()` de `init_db.py`, fase 22 | itens (b) e (c) da `prova_migrate_silencioso.py` (stdout limpo na 2a rodada) e item 22.4 (migração inválida ainda reporta) | | pendente |
| Escada_Preguica_de_Codigo | "5. Dependencia ja instalada? -> usa" | escrever consulta a `information_schema` a mão quando o `inspect(engine)` do SQLAlchemy já vem no projeto | fase 22 | `grep -n "inspect" backend/app/init_db.py` | | pendente |
| TDD_RED_GREEN_REFACTOR | "escreve-se o teste ANTES do codigo. RED -> GREEN -> REFACTOR" | prova escrita depois do conserto | fase 22 | `prova_migrate_silencioso.py` com exit 1 antes e 0 depois, as duas saídas coladas no LOG | | pendente |
